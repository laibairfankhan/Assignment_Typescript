//Intentional Error: If you haven’t received an array index error in one of your programs yet, try to make one happen. Change an index in one of your programs to produce an index error. Make sure you correct the error before closing the program.

//creat an array.

const errorarry: string[] = [` I `, ` You `,` We ` ,` They `];

//Producing Error! by Accessing invalid index Of the Array.
// Uncommenting the line below will cause an index error.
//console.log(errorarry[6]);

//Accessing a valid index to avoid an error.
console.log (errorarry[0]);